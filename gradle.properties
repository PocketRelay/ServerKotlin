systemProp.org.gradle.internal.launcher.welcomeMessageEnabled=false
kotlin.code.style=official

# The version of KME3 this is compiled into the app
kme3Version=1.0.1

# This is the name of the jar file that will be produced when app:shadowJar is
# run. Note: If you are planning on using this in a docker image you will need
# to update the file name inside of Dockerfile aswell
outputJarFile=server.jar

# The version of Java to compile the source code for
# by default this is Java 17 if you require the server
# to run on a different version of java you can change
# it here. Note: other versions are untested and may not
# compile due to missing java features.
javaCompileVersion=17

# Shadow gradle plugin version
shadowVersion=7.1.2

# Netty Dependency Versions
# io.netty:netty-handler
# io.netty:netty-buffer
# io.netty:netty-codec-http
nettyVersion=4.1.79.Final

# Kotlin Version
kotlinVersion=1.7.10

# Kotlin Poet Versions
# com.squareup:kotlinpoet-ksp:
# com.squareup:kotlinpoet
kotlinPoetVersion=1.12.0

# Google KSP Version (com.google.devtools.ksp:symbol-processing-api)
# Make sure to update then when changing the kotlin version
kspVersion=1.7.10-1.0.6

# JSON Serialization (org.jetbrains.kotlinx:kotlinx-serialization-json)
kotlinxSerializationJson=1.3.3

# Exposed Database Dependency Versions:
# org.jetbrains.exposed:exposed-core
# org.jetbrains.exposed:exposed-dao
# org.jetbrains.exposed:exposed-jdbc
exposedVersion=0.38.2